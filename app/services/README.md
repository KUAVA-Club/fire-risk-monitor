# package services/

Contains services that are needed in project such as API request for weather readings. For each API, seperate file should be created. Now, data is only fetched from OpenMeteo API.